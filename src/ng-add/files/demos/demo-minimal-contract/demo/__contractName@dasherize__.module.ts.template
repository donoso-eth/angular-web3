import { NgModule,InjectionToken } from '@angular/core';
import { CommonModule } from '@angular/common';
import { <%= capitalize(contractName) %>Component } from './<%= dasherize(contractName) %>/<%= dasherize(contractName) %>.component';





@NgModule({
  declarations: [
    <%= capitalize(contractName)%>Component
  ],
  imports: [
    CommonModule,
  ],
  exports: [
    <%= capitalize(contractName)%>Component,
  ],
  providers:[]
})
export class <%= capitalize(contractName) %>Module { }
